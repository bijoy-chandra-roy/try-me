/** Stable media viewport so upload / camera / preview / result keep the modal height consistent on mobile. */
export const TRY_ON_MEDIA_FRAME_CLASS =
  'relative flex h-[min(40dvh,16rem)] w-full items-center justify-center overflow-hidden rounded-xl ring-1 ring-[var(--color-border)]';
